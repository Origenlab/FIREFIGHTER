---
import DirectoryLayout from '../../layouts/DirectoryLayout.astro';
import states from '../../data/states.json';

// Sort states alphabetically
const sortedStates = [...states].sort((a, b) => a.name.localeCompare(b.name));
const totalStations = sortedStates.reduce((acc, s) => acc + s.totalStations, 0);

// Group by region
const regions: Record<string, typeof states> = {};
sortedStates.forEach(state => {
  if (!regions[state.region]) {
    regions[state.region] = [];
  }
  regions[state.region].push(state);
});

const regionNames: Record<string, string> = {
  'noroeste': 'Noroeste',
  'norte': 'Norte',
  'noreste': 'Noreste',
  'centro-occidente': 'Centro-Occidente',
  'centro': 'Centro',
  'sur': 'Sur',
  'sureste': 'Sureste',
  'peninsula': 'Península',
};
---

<DirectoryLayout
  title="Directorio Nacional de Estaciones de Bomberos"
  description="Explora el directorio completo de estaciones de bomberos en los 32 estados de México. Encuentra información de contacto, ubicación y servicios."
  breadcrumbs={[{ label: 'Directorio' }]}
>
  <!-- Header Section -->
  <div class="mb-12">
    <h1 class="text-3xl md:text-4xl lg:text-5xl font-display font-bold text-slate-900 mb-6">
      Directorio Nacional de Bomberos
    </h1>

    <!-- Marketing/SEO Content -->
    <div class="grid lg:grid-cols-2 gap-6">
      <p class="text-slate-600 text-lg leading-relaxed">
        Firefighter México es la <strong class="text-slate-900">plataforma de referencia</strong> para localizar estaciones de bomberos en toda la República Mexicana. Nuestro directorio reúne información detallada de más de {totalStations} estaciones distribuidas en los 32 estados, incluyendo datos de contacto directo, ubicación exacta con coordenadas GPS, servicios especializados y horarios de atención.
      </p>
      <p class="text-slate-600 text-lg leading-relaxed">
        Cada ficha de estación incluye información sobre el <strong class="text-slate-900">tipo de cuerpo de bomberos</strong> (profesional, voluntario, industrial o aeroportuario), su equipamiento, áreas de cobertura y servicios de emergencia disponibles. Facilitamos que ciudadanos, empresas e instituciones puedan contactar rápidamente a la estación más cercana en caso de emergencia o para solicitar servicios de prevención.
      </p>
    </div>

    <!-- Search Bar -->
    <div class="bg-white border-2 border-slate-200 rounded-2xl p-2 max-w-2xl mt-8">
      <form action="/directorio" method="GET" class="flex items-center">
        <svg class="w-5 h-5 ml-4 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
        </svg>
        <input
          type="search"
          name="q"
          placeholder="Buscar por estado, ciudad o nombre de estación..."
          class="flex-1 px-4 py-3 text-slate-800 placeholder-slate-400 bg-transparent border-0 focus:outline-none focus:ring-0"
        />
        <button type="submit" class="px-6 py-3 bg-fire-600 text-white font-semibold rounded-xl hover:bg-fire-700 transition-colors">
          Buscar
        </button>
      </form>
    </div>
  </div>

  <!-- States Grid by Region -->
  <div class="space-y-12">
    {Object.entries(regions).map(([region, stateList]) => (
      <div>
        <div class="flex items-center gap-3 mb-6">
          <h2 class="text-xl font-display font-bold text-slate-900">
            {regionNames[region] || region}
          </h2>
          <div class="flex-1 h-px bg-slate-200"></div>
          <span class="text-sm text-slate-500 font-medium">{stateList.length} estados</span>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
          {stateList.map((state) => (
            <a
              href={`/directorio/${state.slug}`}
              class="group bg-white border-2 border-slate-200 rounded-xl p-5 hover:border-fire-400 transition-colors"
            >
              <div class="flex items-start justify-between mb-3">
                <div class="w-10 h-10 bg-fire-100 rounded-lg flex items-center justify-center">
                  <svg class="w-5 h-5 text-fire-600" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M12 2C12 2 7 8 7 13C7 16.866 9.23858 20 12 20C14.7614 20 17 16.866 17 13C17 8 12 2 12 2Z"/>
                  </svg>
                </div>
                <span class="text-xs font-bold text-fire-600 bg-fire-50 px-2 py-1 rounded-full">
                  {state.totalStations}
                </span>
              </div>
              <h3 class="font-display font-bold text-slate-900 group-hover:text-fire-600 transition-colors mb-1">
                {state.name}
              </h3>
              <p class="text-sm text-slate-500">
                {state.capital}
              </p>
            </a>
          ))}
        </div>
      </div>
    ))}
  </div>

  <!-- Emergency CTA -->
  <div class="mt-16 bg-slate-900 rounded-2xl p-8 md:p-12">
    <div class="flex flex-col md:flex-row items-center justify-between gap-6">
      <div class="text-center md:text-left">
        <h2 class="text-2xl md:text-3xl font-display font-bold text-white mb-3">
          ¿Tienes una emergencia?
        </h2>
        <p class="text-slate-400 text-lg max-w-lg">
          No pierdas tiempo buscando. Llama inmediatamente al número de emergencias nacional.
        </p>
      </div>
      <a
        href="tel:911"
        class="flex items-center gap-3 px-8 py-5 bg-fire-600 text-white font-bold text-lg rounded-xl hover:bg-fire-700 transition-colors whitespace-nowrap"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
        </svg>
        Llamar al 911
      </a>
    </div>
  </div>
</DirectoryLayout>
