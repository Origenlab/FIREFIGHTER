---
import BaseLayout from '@layouts/BaseLayout.astro';
---

<BaseLayout
  title="Contacto"
  description="Contáctanos para cotizaciones, dudas o más información sobre nuestros servicios de protección contra incendios."
  keywords={['contacto', 'cotización', 'información']}
>
  <section class="section">
    <div class="container">
      <div class="contact-grid">
        <div class="contact-info">
          <h1>Contáctanos</h1>
          <p class="lead">
            Estamos aquí para ayudarte. Completa el formulario o utiliza nuestros datos de contacto directo.
          </p>

          <div class="info-cards">
            <div class="info-card">
              <h3>📍 Dirección</h3>
              <address>
                <p>Ciudad de México, México</p>
              </address>
            </div>

            <div class="info-card">
              <h3>📞 Teléfono</h3>
              <p><a href="tel:+525512345678">+52 55 1234 5678</a></p>
            </div>

            <div class="info-card">
              <h3>✉️ Email</h3>
              <p><a href="mailto:info@firefighter.com.mx">info@firefighter.com.mx</a></p>
            </div>

            <div class="info-card">
              <h3>🕐 Horario</h3>
              <p>Lunes a Viernes: 9:00 - 18:00</p>
              <p>Emergencias: 24/7</p>
            </div>
          </div>
        </div>

        <div class="contact-form-wrapper">
          <form class="contact-form card" name="contact" method="POST" data-netlify="true">
            <div class="card-content">
              <h2>Envíanos un mensaje</h2>

              <div class="form-group">
                <label for="name" class="form-label">Nombre completo *</label>
                <input
                  type="text"
                  id="name"
                  name="name"
                  class="form-input"
                  required
                  autocomplete="name"
                />
              </div>

              <div class="form-group">
                <label for="email" class="form-label">Correo electrónico *</label>
                <input
                  type="email"
                  id="email"
                  name="email"
                  class="form-input"
                  required
                  autocomplete="email"
                />
              </div>

              <div class="form-group">
                <label for="phone" class="form-label">Teléfono</label>
                <input
                  type="tel"
                  id="phone"
                  name="phone"
                  class="form-input"
                  autocomplete="tel"
                />
              </div>

              <div class="form-group">
                <label for="service" class="form-label">Servicio de interés</label>
                <select id="service" name="service" class="form-input">
                  <option value="">Selecciona una opción</option>
                  <option value="extintores">Extintores</option>
                  <option value="sistemas">Sistemas contra incendios</option>
                  <option value="capacitacion">Capacitación</option>
                  <option value="otro">Otro</option>
                </select>
              </div>

              <div class="form-group">
                <label for="message" class="form-label">Mensaje *</label>
                <textarea
                  id="message"
                  name="message"
                  class="form-textarea"
                  rows="5"
                  required
                ></textarea>
              </div>

              <button type="submit" class="btn btn-primary btn-block">
                Enviar mensaje
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .contact-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-12);
    align-items: start;
  }

  h1 {
    margin-bottom: var(--space-4);
  }

  .lead {
    color: var(--color-text-secondary);
    font-size: var(--text-lg);
    margin-bottom: var(--space-8);
  }

  .info-cards {
    display: grid;
    gap: var(--space-4);
  }

  .info-card {
    padding: var(--space-4);
    background-color: var(--color-bg-secondary);
    border-radius: var(--radius-lg);
  }

  .info-card h3 {
    font-size: var(--text-base);
    margin-bottom: var(--space-2);
  }

  .info-card p {
    margin-bottom: var(--space-1);
    color: var(--color-text-secondary);
  }

  .info-card address {
    font-style: normal;
  }

  .contact-form h2 {
    margin-bottom: var(--space-6);
  }

  .btn-block {
    width: 100%;
  }

  @media (max-width: 768px) {
    .contact-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
